<?php 
require_once('menu.php');

    /*
    include -> quando não existir o documento é retornando um warning e é exibido o resto
    da página. Ele pode ser chamado e exibido em varias partes do código

    include_once() -> Só realiza uma chamada do documento
    
    require -> quando não existir o documento é retornado um fatal erro e não é exibido 
    o resto da página

     require_once() -> Só realiza uma chamada do documento

    */

?>

<h3>Conteúdo da index</h3>